//bucle while primer ejercicio
//mostrar numeros del 1 al 10
#include <stdio.h>
int main(){
    //el bucle while
    int numeroInicial =0; 
    int numeroFinal=10000;
    //nombre de3l cbluce parentis para la condicion
    //entre llaves el codigo a ejecutar
    while(numeroFinal >= numeroInicial){
        printf("%d\n",numeroFinal);
        numeroFinal=  numeroFinal-2; //operador de incremento
        //suma 1 al valor de numeroInicial
    }
return 0;

}